# 처음부터 이해하는 Mission 1-1

아래 답변은 **공개된 과제 요구사항을 바탕으로 한 연습용**입니다. 실제 동료 평가 항목은 아직 확인하지 않았습니다. 문장을 외우기보다 해당 화면과 코드를 같이 보세요.

## 1. 우선 세 파일만 구분하기

**HTML은 무엇이 있는지**, **CSS는 어떻게 보이는지**, **JavaScript는 행동에 어떻게 반응하는지**를 담당합니다.

예를 들어 다크 모드 버튼은 `index.html`에 있습니다. 색상은 `css/style.css`에 있습니다. 버튼을 누르면 색상 설정을 바꾸는 일은 `js/main.js`가 합니다. `js/config.js`는 사이트에 표시할 이름과 GitHub 아이디만 담습니다.

Python 경험에 연결하면 JavaScript의 객체는 딕셔너리와, 배열은 리스트와 비슷한 역할을 합니다. `const`는 변수 이름에 다른 값을 재할당하지 못하게 하지만, 객체 내부 값까지 변경 불가능하게 만들지는 않습니다. 따라서 `const state = {...}` 다음에 `state.theme = 'dark'`는 가능합니다. `let`은 재할당할 값에 사용합니다.

## 2. DOM과 이벤트

DOM은 브라우저가 HTML을 읽어 만든 요소들의 트리입니다. JavaScript에서 화면 요소를 선택하고 수정할 수 있습니다.

```js
const themeButton = document.querySelector('#theme-toggle');
```

`#`은 HTML의 id를 찾는 선택자입니다. `querySelector`는 첫 번째 요소 하나를, `querySelectorAll`은 조건에 맞는 요소들을 선택합니다.

```js
themeButton.addEventListener('click', () => {
  state.theme = state.theme === 'light' ? 'dark' : 'light';
  renderTheme();
});
```

`addEventListener`는 지금 클릭하는 함수가 아니라 **나중에 클릭되면 실행할 함수**를 연결합니다. `() => {...}`는 화살표 함수입니다. `조건 ? A : B`는 조건이 참이면 A, 거짓이면 B를 고릅니다.

## 3. 이번 과제의 가장 중요한 흐름

버튼을 누르는 사건이 **이벤트**, 현재 테마 값이 **상태**, 상태에 맞게 화면을 바꾸는 과정이 **렌더링**입니다.

1. 사용자가 다크 모드 버튼을 누릅니다.
2. `state.theme`가 `light`에서 `dark`로 바뀝니다.
3. `renderTheme()`가 `<html data-theme="dark">`로 바꿉니다.
4. CSS의 `[data-theme="dark"]` 규칙이 색상 변수를 바꿉니다.
5. `localStorage`에도 저장합니다. 새로고침 때 이 값을 읽습니다.

**직접 확인:** 버튼 클릭 → 새로고침 → 테마 유지. 개발자 도구에서 html 요소의 `data-theme`을 확인하세요.

## 4. HTML을 설명하기

**예상 질문: 왜 div만 쓰지 않았나요?**

답변 예시: “문서의 역할을 표현하려고 시맨틱 태그를 사용했습니다. 상단은 header, 이동 메뉴는 nav, 주 콘텐츠는 main, 소개와 프로젝트 영역은 section, 독립적인 카드에는 article, 하단에는 footer를 사용했습니다. 구조를 읽기 쉽고 보조 기술도 문서의 영역을 이해하는 데 도움이 됩니다.”

`label for="email"`과 `input id="email"`을 연결하면 레이블을 눌러 입력란으로 이동할 수 있습니다. 이미지 `alt`는 이미지를 볼 수 없을 때 전달할 의미입니다. 제목은 페이지의 h1 아래에 섹션별 h2, 카드별 h3를 배치했습니다.

`defer`는 HTML 파싱과 병행하여 스크립트를 내려받고, HTML 파싱이 끝난 뒤 실행하게 합니다. 먼저 `config.js`, 다음 `main.js` 순서도 유지합니다.

## 5. CSS를 설명하기

**예상 질문: Flexbox와 Grid는 무엇이 다른가요?**

답변 예시: “Flexbox는 한 축을 중심으로 요소를 배치할 때 편해서 내비게이션의 로고와 메뉴 정렬에 사용했습니다. Grid는 행과 열로 카드들을 배치할 때 편해서 프로젝트 목록에 사용했습니다. auto-fit과 minmax를 조합해 공간에 맞게 카드 열 수가 바뀌도록 했습니다.”

기본 스타일은 모바일에 맞춥니다. `min-width: 768px`에서 태블릿·데스크톱 내비게이션과 두 열 배치를 적용하고, `1024px`에서 기술 카드 네 열과 큰 화면 배치를 적용합니다.

`:root`의 `--accent` 같은 CSS 변수는 반복되는 색상을 한곳에서 관리합니다. 다크 모드는 각 요소를 직접 다시 칠하는 대신 변수 값을 바꿉니다. `:hover`는 마우스를 올렸을 때, `transition`은 변화가 이어지는 시간을 지정합니다.

## 6. 메뉴, 스크롤, 애니메이션

- 메뉴 클릭 → `state.menuOpen` 변경 → `classList.toggle('active', state.menuOpen)` → CSS로 표시/숨김.
- 스크롤 이벤트 → `scrollY` 확인 → 60px 이상이면 헤더의 `scrolled` 클래스 적용.
- 300px 이상이면 맨 위 버튼의 `hidden`을 해제.
- 앵커 링크는 `#projects` 같은 id로 이동. CSS `scroll-behavior: smooth`로 부드럽게 이동.
- `IntersectionObserver`는 요소와 화면의 겹침을 관찰. threshold 0.2는 관찰 대상의 20%가 보이는 경계이며, 요소가 진입하면 숨김 클래스를 제거.

애니메이션이 한 번 실행되면 `unobserve`로 관찰을 종료합니다. 기기의 움직임 감소 설정도 존중합니다.

## 7. API와 비동기

API는 다른 서비스와 약속된 방법으로 데이터를 주고받는 인터페이스입니다. 이 과제에서는 GitHub가 공개 저장소 목록을 JSON 형식으로 제공합니다.

`loadProjects()` 읽는 순서:

1. 상태를 `loading`으로 바꾸고 로딩 문구 표시.
2. `await fetch(...)`로 응답을 기다림. 기다리는 동안 다른 사용자 조작은 가능.
3. `response.ok` 확인. fetch는 404, 403이어도 자동으로 예외를 던지지 않으므로 직접 검사.
4. `await response.json()`으로 응답 본문을 JavaScript 값으로 변환.
5. 성공 상태와 받은 배열을 저장하고 `renderProjects()` 실행.
6. 오류가 나면 `catch`에서 오류 상태로 바꾸고 재시도 버튼 표시.

**예상 질문: 로딩·실패·빈 상태는 왜 필요한가요?**

답변 예시: “요청 중에는 기다리는 중임을 알려주고, 요청에 실패하면 원인 안내와 재시도를 제공합니다. 성공했어도 목록이 비어 있을 수 있으므로 오류와 구분해서 표시합니다.”

401/403 같은 숫자는 HTTP 상태 코드입니다. 이번 구현에서는 403/429에 요청 제한 안내를 표시합니다. 타임아웃은 무한히 기다리지 않도록 두었습니다.

## 8. map, filter, forEach와 문자열

```js
const { status, items, error } = state.projects;
```

구조분해 할당으로 객체의 속성을 같은 이름의 변수로 꺼냅니다.

- `map`: 각 저장소를 HTML 카드 문자열로 바꾸어 **새 배열**을 만듭니다.
- `join('')`: 카드 문자열 배열을 한 문자열로 합칩니다.
- `forEach`: 각 입력란이나 버튼에 반복 작업을 합니다. 변환된 배열을 반환할 목적이 아닙니다.
- `filter`: 조건에 맞는 항목만 모아 새 배열을 만듭니다. 이 코드에서는 오류가 있는 입력란을 골라냅니다. 프로젝트 언어별 필터는 선택 과제로 구현하지 않았습니다.
- 백틱으로 감싼 템플릿 리터럴의 `${값}` 위치에 값을 넣어 HTML을 만듭니다.

`textContent`는 일반 텍스트를 넣고 `innerHTML`은 문자열을 HTML로 해석합니다. GitHub 설명을 그대로 innerHTML에 넣으면 태그로 해석될 수 있으므로 `escapeHTML`로 변환합니다.

## 9. 문의 폼

**예상 질문: preventDefault는 왜 사용했나요?**

답변 예시: “폼 제출의 기본 동작인 페이지 이동을 막고 현재 화면에서 입력 검증 결과를 보여주기 위해 사용했습니다. 빈칸과 공백만 있는 값, 잘못된 이메일 형식을 검사하고 오류가 있는 필드 가까이에 안내를 표시합니다.”

`input` 이벤트는 입력이 바뀔 때 검증합니다. `submit` 이벤트에서는 모든 필드를 검사합니다. 오류가 있으면 첫 오류 입력란으로 포커스를 옮깁니다. 서버가 없으므로 실제 이메일은 보내지 않고 검증 성공만 표시합니다. `novalidate`는 브라우저 기본 팝업 대신 직접 만든 오류 안내를 사용하기 위한 설정입니다.

## 10. 스스로 해볼 작은 수정

1. `config.js`의 name을 원하는 표시 이름으로 바꾸고 새로고침.
2. CSS의 `--accent`를 바꾸어 어떤 요소가 함께 변하는지 관찰.
3. 맨 위 버튼 기준을 300에서 500으로 바꾸고 차이 확인 후 원복.
4. 문의 폼에 공백만 입력해 거부되는지 확인.
5. `renderTheme()` 호출을 잠깐 주석 처리하고 버튼 클릭 결과를 관찰 후 원복. 상태만 바꾸어서는 화면이 바뀌지 않는다는 점을 이해.

## 11. 2분 시연 순서

1. 전체 섹션 소개와 HTML/CSS/JS 역할 설명.
2. 작은 화면에서 햄버거 메뉴 열기/닫기.
3. 다크 모드 전환 후 새로고침하여 유지 확인.
4. 프로젝트 링크와 실제 API 데이터 설명.
5. 빈 폼 제출 → 오류, 올바른 입력 → 성공 안내 시연.
6. `state`, `renderTheme`, `loadProjects`, `renderForm`을 보여주며 이벤트 → 상태 → 렌더링 설명.

실제 평가항목이 공개되면 이 문서를 기준으로 질문별 설명과 시연 위치를 다시 맞춥니다.
